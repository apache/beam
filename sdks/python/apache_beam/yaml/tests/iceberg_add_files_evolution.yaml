#
# Licensed to the Apache Software Foundation (ASF) under one or more
# contributor license agreements.  See the NOTICE file distributed with
# this work for additional information regarding copyright ownership.
# The ASF licenses this file to You under the Apache License, Version 2.0
# (the "License"); you may not use this file except in compliance with
# the License.  You may obtain a copy of the License at
#
#    http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.
#

fixtures:
  - name: TEMP_DIR
    type: "tempfile.TemporaryDirectory"


pipelines:
  # Pipeline 1: files with two columns
  - pipeline:
      type: chain
      transforms:
        - type: Create
          config:
            elements:
              - {label: "11a", rank: 0}
              - {label: "37a", rank: 1}
        - type: WriteToParquet
          config:
            path: "{TEMP_DIR}/data/narrow"
            file_name_suffix: ".parquet"
            num_shards: 1

  # Pipeline 2: files with an extra column
  - pipeline:
      type: chain
      transforms:
        - type: Create
          config:
            elements:
              - {label: "389a", rank: 2, bool: false}
              - {label: "3821b", rank: 3, bool: true}
        - type: WriteToParquet
          config:
            path: "{TEMP_DIR}/data/wide"
            file_name_suffix: ".parquet"
            num_shards: 1

  # Pipeline 3: register both; the table is created from the union of the file schemas
  - pipeline:
      type: chain
      transforms:
        - type: ReadMatchFiles
          config:
            file_pattern: "{TEMP_DIR}/data/*.parquet"
        - type: MapToFields
          config:
            language: python
            fields:
              path:
                callable: "lambda row: row.path"
                output_type: string
        - type: IcebergAddFiles
          config:
            table: "default.table"
            catalog_properties:
              type: "hadoop"
              warehouse: "{TEMP_DIR}/dir"
            schema_evolution_options: [ALLOW_FIELD_ADDITION, ALLOW_FIELD_RELAXATION]

    providers:
      - type: python
        config: { }
        transforms:
          ReadMatchFiles: 'apache_beam.io.fileio.MatchFiles'

  # Pipeline 4: every file is readable through the evolved schema
  - pipeline:
      type: chain
      transforms:
        - type: ReadFromIceberg
          config:
            table: "default.table"
            catalog_properties:
              type: "hadoop"
              warehouse: "{TEMP_DIR}/dir"
        - type: AssertEqual
          config:
            elements:
              - {label: "11a", rank: 0, bool: null}
              - {label: "37a", rank: 1, bool: null}
              - {label: "389a", rank: 2, bool: false}
              - {label: "3821b", rank: 3, bool: true}
