name: simple_pipeline
version: 1.0

profile: simple_pipeline


models:
  python:
    +materialized: table
  wins_by_id:
    +materialized: table
  example:
    simple_aggregation:
      +materialized: table
