/*
 * Licensed to the Apache Software Foundation (ASF) under one
 * or more contributor license agreements.  See the NOTICE file
 * distributed with this work for additional information
 * regarding copyright ownership.  The ASF licenses this file
 * to you under the Apache License, Version 2.0 (the
 * License); you may not use this file except in compliance
 * with the License.  You may obtain a copy of the License at
 *
 *     http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an AS IS BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */
import groovy.json.JsonOutput

plugins { id 'org.apache.beam.module' }
applyJavaNature(
        automaticModuleName: 'org.apache.beam.sdk.io.debezium',
        mavenRepositories: [
                [id: 'io.confluent', url: 'https://packages.confluent.io/maven/']
        ],
        enableSpotbugs: false,
)
provideIntegrationTestingDependencies()

description = "Apache Beam :: SDKs :: Java :: IO :: Debezium"
ext.summary = "Library to work with Debezium data."

dependencies {
    implementation library.java.vendored_guava_32_1_2_jre
    implementation library.java.vendored_grpc_1_69_0
    implementation project(path: ":sdks:java:core", configuration: "shadow")
    implementation library.java.slf4j_api
    implementation library.java.joda_time
    provided library.java.jackson_dataformat_csv
    permitUnusedDeclared library.java.jackson_dataformat_csv
    testImplementation project(path: ":sdks:java:core", configuration: "shadowTest")
    testImplementation project(path: ":sdks:java:io:common")

    // Test dependencies
    testImplementation library.java.junit
    testImplementation project(path: ":sdks:java:io:jdbc")
    testRuntimeOnly library.java.slf4j_jdk14
    testRuntimeOnly project(path: ":runners:direct-java", configuration: "shadow")
    testImplementation project(":runners:google-cloud-dataflow-java")
    testImplementation library.java.hamcrest
    testImplementation library.java.testcontainers_base
    testImplementation library.java.testcontainers_mysql
    testImplementation library.java.testcontainers_postgresql
    // TODO(https://github.com/apache/beam/issues/31678) HikariCP 5.x requires Java11+
    testImplementation 'com.zaxxer:HikariCP:4.0.3'

    // Kafka connect dependencies
    implementation "org.apache.kafka:connect-api:2.5.0"
    implementation "org.apache.kafka:connect-json:2.5.0"
    permitUnusedDeclared "org.apache.kafka:connect-json:2.5.0" // BEAM-11761

    // Debezium dependencies
    implementation group: 'io.debezium', name: 'debezium-core', version: '3.1.1.Final'
    testImplementation group: 'io.debezium', name: 'debezium-connector-mysql', version: '3.1.1.Final'
    testImplementation group: 'io.debezium', name: 'debezium-connector-postgres', version: '3.1.1.Final'
}

test {
    testLogging {
        outputs.upToDateWhen {false}
        showStandardStreams = true
    }
}

sourceCompatibility = JavaVersion.VERSION_17
targetCompatibility = JavaVersion.VERSION_17

tasks.withType(JavaCompile).configureEach { JavaCompile task ->
    if (task.project == project && (task.name == 'compileJava' || task.name == 'compileTestJava')) {
        logger.lifecycle("[BUILD_GRADLE] Debezium IO: Configuring ${task.path} for Java 17.")
        String jdk17HomePath = project.findProperty("java17Home")?.toString()?.trim()

        if (jdk17HomePath != null && !jdk17HomePath.isEmpty() && project.file(jdk17HomePath).isDirectory()) {
            logger.lifecycle("[BUILD_GRADLE] Debezium IO: Using java17Home='${jdk17HomePath}' for ${task.path}.")
            task.options.fork = true
            task.options.forkOptions.javaHome = project.file(jdk17HomePath)

            // Step 1: Set base compiler args including --release 17.
            List<String> initialCompilerArgs = []
            task.options.compilerArgs.each { argObject ->
                String arg = argObject.toString()
                if (arg.startsWith('-Xlint') || arg == '-parameters' || arg == '-Werror' || arg.startsWith("-XD")) {
                    initialCompilerArgs.add(arg)
                }
            }
            initialCompilerArgs.add('--release')
            initialCompilerArgs.add('17')
            task.options.compilerArgs = initialCompilerArgs
            logger.lifecycle("[BUILD_GRADLE] Debezium IO: ${task.path} Initial compilerArgs set to: ${task.options.compilerArgs.join(' ')}")

            // Step 2: Configure JVM args for the forked JVM to allow ErrorProne access.
            if (project.plugins.hasPlugin('net.ltgt.errorprone')) {
                def errorProneAddModuleOptsForJava17 = [
                    "--add-exports=jdk.compiler/com.sun.tools.javac.api=ALL-UNNAMED",
                    "--add-exports=jdk.compiler/com.sun.tools.javac.file=ALL-UNNAMED",
                    "--add-exports=jdk.compiler/com.sun.tools.javac.main=ALL-UNNAMED",
                    "--add-exports=jdk.compiler/com.sun.tools.javac.model=ALL-UNNAMED",
                    "--add-exports=jdk.compiler/com.sun.tools.javac.parser=ALL-UNNAMED",
                    "--add-exports=jdk.compiler/com.sun.tools.javac.processing=ALL-UNNAMED",
                    "--add-exports=jdk.compiler/com.sun.tools.javac.tree=ALL-UNNAMED",
                    "--add-exports=jdk.compiler/com.sun.tools.javac.util=ALL-UNNAMED",
                    "--add-opens=jdk.compiler/com.sun.tools.javac.code=ALL-UNNAMED",
                    "--add-opens=jdk.compiler/com.sun.tools.javac.comp=ALL-UNNAMED"
                ]
                
                // Get existing JVM args, but filter out any problematic jdk.compiler args without -J
                List<String> existingJvmArgs = task.options.forkOptions.getJvmArgs() ?: []
                List<String> cleanJvmArgs = []
                existingJvmArgs.each { ejArg ->
                    if (!ejArg.contains("jdk.compiler")) { // Keep other unrelated JVM args
                        cleanJvmArgs.add(ejArg)
                    }
                }
                
                // Add ONLY the -J prefixed versions for ErrorProne
                errorProneAddModuleOptsForJava17.collect { '-J' + it }.each { argToAdd ->
                    if (!cleanJvmArgs.contains(argToAdd)) { 
                        cleanJvmArgs.add(argToAdd) 
                    }
                }
                task.options.forkOptions.setJvmArgs(cleanJvmArgs) // Use the cleaned and J-prefixed list
                logger.lifecycle("[BUILD_GRADLE] Debezium IO: ${task.path} - Set STRICT forkOptions.jvmArgs for ErrorProne: ${task.options.forkOptions.jvmArgs.join(' ')}")
            } else {
                 logger.warn("[BUILD_GRADLE] Debezium IO: ${task.path} - ErrorProne plugin not detected. Skipping ErrorProne JVM args specific setup.")
            }

            // Step 3: Use doFirst to clean up compilerArgs (arguments for javac itself)
            task.doFirst {
                logger.lifecycle("[BUILD_GRADLE] Debezium IO: ${task.path} - In doFirst. Current compilerArgs from task.options: ${task.options.compilerArgs.join(' ')}")
                List<String> cleanedCompilerArgs = []
                boolean skipNextInDoFirst = false
                for (String currentArg : task.options.compilerArgs) {
                    if (skipNextInDoFirst) {
                        skipNextInDoFirst = false; continue;
                    }
                    if (currentArg.startsWith("--add-exports=") || currentArg.startsWith("--add-opens=")) {
                        logger.lifecycle("[BUILD_GRADLE] Debezium IO: ${task.path} - Removing direct ${currentArg} from compilerArgs."); continue;
                    }
                    if (currentArg == "--add-exports" || currentArg == "--add-opens") {
                        logger.lifecycle("[BUILD_GRADLE] Debezium IO: ${task.path} - Removing split ${currentArg} and its value."); skipNextInDoFirst = true; continue;
                    }
                    cleanedCompilerArgs.add(currentArg)
                }

                boolean releaseFlagPresent = false
                boolean correctReleaseValue = false
                for(int i=0; i < cleanedCompilerArgs.size(); i++) {
                    if(cleanedCompilerArgs.get(i) == '--release') {
                        releaseFlagPresent = true
                        if (i + 1 < cleanedCompilerArgs.size() && cleanedCompilerArgs.get(i + 1) == '17') {
                            correctReleaseValue = true
                        }
                        break 
                    }
                }
                if (!releaseFlagPresent || !correctReleaseValue) {
                    logger.warn("[BUILD_GRADLE] Debezium IO: ${task.path} - Ensuring --release 17. Was present: ${releaseFlagPresent}, Was correct: ${correctReleaseValue}. Re-adding/Correcting.")
                    List<String> tempArgs = []
                    boolean skipRelease = false
                    for (String arg : cleanedCompilerArgs) {
                        if (skipRelease) { skipRelease = false; continue; }
                        if (arg == '--release') { skipRelease = true; continue; }
                        tempArgs.add(arg)
                    }
                    cleanedCompilerArgs = tempArgs
                    cleanedCompilerArgs.add('--release')
                    cleanedCompilerArgs.add('17')
                }

                task.options.compilerArgs = cleanedCompilerArgs
                logger.lifecycle("[BUILD_GRADLE] Debezium IO: ${task.path} - Final compilerArgs set in doFirst: ${task.options.compilerArgs.join(' ')}")
            }
        } else {
            // Fallback if java17Home is not found - simplified
            logger.warn("[BUILD_GRADLE] Debezium IO: Project property 'java17Home' not found for ${task.path}. Applying default --release 17 to compiler args.")
            List<String> existingArgs = task.options.compilerArgs.collect { it.toString() }
            List<String> finalCompilerArgs = []
            existingArgs.each { arg ->
                if (arg.startsWith('-Xlint') || arg == '-parameters' || arg == '-Werror' || arg.startsWith("-XD")) {
                    finalCompilerArgs.add(arg)
                }
            }
            finalCompilerArgs.add('--release')
            finalCompilerArgs.add('17')
            task.options.compilerArgs = finalCompilerArgs
        }
    }
}

// Configure Javadoc task to also use JDK 17
tasks.named('javadoc', Javadoc) {
    logger.lifecycle("[BUILD_GRADLE] Debezium IO: Attempting to configure javadoc task for Java 17.")
    dependsOn(tasks.named('classes'))

    classpath = sourceSets.main.allJava.sourceDirectories + sourceSets.main.compileClasspath + sourceSets.main.output

    String jdk17HomePath = project.findProperty("java17Home")?.toString()?.trim()
    if (jdk17HomePath != null && !jdk17HomePath.isEmpty() && project.file(jdk17HomePath).isDirectory()) {
        logger.lifecycle("[BUILD_GRADLE] Debezium IO: Using java17Home='${jdk17HomePath}' for javadoc executable.")
        executable = project.file("${jdk17HomePath}/bin/javadoc").absolutePath
    } else {
        logger.warn("[BUILD_GRADLE] Debezium IO: 'java17Home' not found for javadoc task. Using default javadoc tool.")
    }
}

task integrationTest(type: Test, dependsOn: processTestResources) {
  group = "Verification"
  systemProperty "beamTestPipelineOptions", JsonOutput.toJson([
          "--runner=DirectRunner",
  ])

  // Disable Gradle cache: these ITs interact with live service that should always be considered "out of date"
  outputs.upToDateWhen { false }

  include '**/*IT.class'
  classpath = sourceSets.test.runtimeClasspath
  testClassesDirs = sourceSets.test.output.classesDirs

  useJUnit {
  }
}
