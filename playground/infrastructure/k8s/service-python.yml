apiVersion: v1
kind: Service
metadata:
  labels:
    app: backend-python
    type: backend
  name: playground-python
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 80
  selector:
    app: backend-python
  type: LoadBalancer