apiVersion: apps/v1
kind: Deployment
metadata:
  name: playground-go
  labels:
    app: backend-go
    type: backend
spec:
  template:
    metadata:
     name: playground
     labels: 
       app: backend-go
       type: backend
    spec:
     containers: 
     - name: playground-backend-go
       image: us-central1-docker.pkg.dev/apache-beam-testing/playground-repository/beam_playground-backend-go:beta-version-v4
  replicas:1
  selector:
    matchLabels:
     app: backend-go